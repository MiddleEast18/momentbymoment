# تقرير الفحص الشامل لنظام مِرصاد وبنيته التحتية

**تاريخ الفحص:** 16 سبتمبر 2026

**نطاق الفحص:** المستودع `MiddleEast18/momentbymoment` عند الالتزام `e517be8` على الفرع `main`.[1]

## الخلاصة التنفيذية

مِرصاد موقع إخباري عربي ثابت الواجهة يعتمد على GitHub Pages أو استضافة ملفات ثابتة، وعلى JavaScript في المتصفح، وعلى Supabase للمصادقة وقاعدة البيانات ووظائف Edge والمهام المجدولة. ويستخدم n8n كمسار مستقل للتصنيف والتلخيص والتحقق المدعوم بالذكاء الاصطناعي. يتكون المستودع من 82 ملفًا متتبعًا، منها 9 صفحات HTML و16 ملف JavaScript و12 ملف CSS، إضافة إلى 30 ترحيلًا لقاعدة البيانات و8 وظائف Edge.

البنية الحالية قابلة للتشغيل ومتماسكة وظيفيًا في معظم مسارات القراءة والمصادقة والرصيد. كما أن التصميم يحتوي على عناصر جيدة، أهمها استخدام معاملات PostgreSQL الذرّية لاستهلاك الفتحات والمكافآت، وتفعيل سياسات Row Level Security لبعض البيانات الحساسة، وفصل دور محرر الأخبار `news_admin` عن المستخدم العادي.

لكن لا أوصي باعتبار النظام جاهزًا للإنتاج عالي الاعتمادية قبل إغلاق ثلاث نقاط. أولًا، يجب إثبات حماية كل وظيفة Edge ذات امتيازات مرتفعة، وبالأخص `mirsad-ingest`، لأن الكود نفسه لا ينفذ تحققًا صريحًا من هوية المستدعي. ثانيًا، يجب تقييد واجهة `mirsad-analysis-api` بدل CORS المفتوح وعدم تركها بوابة عامة يمكن استخدامها لاستهلاك خدمة التحليل أو تمرير روابط خارجية. ثالثًا، يجب إضافة اختبارات نشر حية ومراقبة دورية لأن المستودع لا يحتوي إعدادات نشر أو اختبارات تكامل أو تعريفًا واضحًا للبيئة التشغيلية.

## تقدير الحالة حسب المجال

| المجال | الحالة | التقييم |
|---|---|---|
| الواجهة الثابتة | تعمل بنيويًا | جيدة مع اعتماد كبير على ملفات JavaScript غير مجمعة |
| المصادقة والملف الشخصي | منطق واضح | جيدة، مع ضرورة اختبار OAuth في البيئة المنشورة |
| قاعدة البيانات وRLS | حماية موجودة ومتقدمة نسبيًا | جيدة جزئيًا، لأن التحقق النهائي يتطلب مخططًا حيًا وصلاحيات فعلية |
| الاستيعاب وRSS | متعدد المصادر ويدعم سجلًا للتكرار | معرض لسوء استخدام نقطة الدخول إن لم تُفرض حماية النشر |
| الأتمتة وn8n | مسار تصنيف وتحقيق واضح | يعتمد على سر مشترك وإعدادات خارجية غير موجودة في المستودع |
| الدفع والفوحات | معاملات ودوال مقيّدة | يحتاج تدقيقًا حيًا للويب هوك ومصدر الحقيقة المالية |
| الاختبارات والمراقبة | فحوصات تركيبية فقط | غير كافية للإنتاج |
| سلسلة التوريد | CDN خارجي بلا تثبيت إصدار أو SRI | مخاطرة متوسطة |

## كيف يعمل النظام

### طبقة العرض

الصفحات HTML ثابتة، وتُحمّل مكتبة Supabase JavaScript من jsDelivr ثم ملفات الواجهة المحلية. الصفحة الرئيسية (`index.html`) تعرض الأخبار[5]، وتستورد طبقات القراءة والترقيم والوصول والمصادقة والأخبار السريعة. ولا توجد في المستودع ملفات بناء مثل `package.json` أو مدير حزم أو إطار واجهة؛ لذلك لا توجد عملية build أو bundle أو lockfile يمكن مراجعتها.

هذا الاختيار يقلل تعقيد الخادم، لكنه يجعل المتصفح يعتمد مباشرة على عدد كبير من الملفات وبطاقات cache-busting يدوية. كما يجعل ضبط الإصدارات واكتشاف التعارض بين الملفات أصعب من تطبيق مبني بخط تجميع موحد.

### المصادقة والوصول

يستخدم `auth-v8.js` عميل Supabase[6] في المتصفح، ويدعم استعادة جلسة OAuth، وتسجيل الدخول عبر Google، ووضع الزائر، وإنشاء الملف الشخصي، ونظام الفتحات والمكافأة اليومية. تُنفذ عمليات استهلاك الرصيد الحساسة عبر RPC في قاعدة البيانات بدل تعديل الرصيد مباشرة من المتصفح، وهذه نقطة تصميم صحيحة.

التفريق بين الزائر والمستخدم المسجل حاضر في الواجهة، كما أن قراءة الأخبار العامة منفصلة عن عمليات التوسعة التي تستهلك فتحات. مع ذلك، لا ينبغي اعتبار حماية الواجهة حماية أمنية بحد ذاتها؛ مصدر الحقيقة يجب أن يبقى في RLS والدوال المقيّدة داخل PostgreSQL.

### البيانات والاستيعاب

تستخدم الهجرات جداول للأخبار والمصادر وسجل عناصر المصدر وحالة التغذية وصحة المصدر وطوابير التحقق وسجل الفتحات والمكافآت.[7] [8] تقوم وظائف RSS بجلب مصادر متعددة، وتطبيع الروابط، ومنع التكرار، وتصنيف المحتوى بقواعد محلية، وتسجيل نتائج الجلب.

توجد وظيفة مجدولة لجلب RSS كل 15 دقيقة. كما توجد عمليات تنظيف وتقليص لسجل الأخبار، مع مزامنة ثنائية الاتجاه بين `news_articles` و`source_item_ledger`. هذه العمليات مفيدة للحفاظ على الحجم، لكنها عمليات حذف فعلية؛ ولذلك يجب وجود نسخة احتياطية أو أرشفة قبل اعتمادها على بيانات إنتاج مهمة.

### n8n والتحليل

يحتوي `n8n-workflow.json` على 16 عقدة.[4] يبدأ المسار بويب هوك `mirsad-ingest`، ثم يتحقق من `x-mirsad-secret`، ويمرر الخبر إلى التصنيف والتلخيص، ثم يجلب سياق الحدث، وينفذ مقارنة للتحقق، وبعدها يكتب إلى Supabase أو يرسل العنصر إلى طابور التحقق.

من حيث المبدأ، فصل مسار الذكاء الاصطناعي عن الواجهة مناسب. لكن ملف Workflow لا يثبت أن الويب هوك منشور فعلًا مع سياسة مصادقة ومعدل طلبات وسجل تدقيق. كما أن استخدام مفاتيح الخدمة داخل n8n يجعل حماية بيئة n8n نفسها جزءًا من سطح الأمان الأساسي.

## النتائج ذات الأولوية

### أولوية حرجة: حماية وظيفة الاستيعاب الرئيسية غير مثبتة في الكود

`supabase/functions/mirsad-ingest/index.ts` ينشئ عميل Supabase[2] باستخدام `SUPABASE_SERVICE_ROLE_KEY` ثم يبدأ المعالجة عند استقبال POST. لا يظهر في بداية الوظيفة تحقق من JWT أو توقيع أو سر مشترك أو قائمة سماح للمستدعين. الوظيفة لا تضع قرار الحماية داخل التطبيق؛ لذلك يعتمد الأمان على إعداد Supabase الخارجي، مثل `verify_jwt`، وعلى أن تكون الوظيفة قابلة للاستدعاء من Cron دون فتحها للعامة.

هذا خطر مرتفع لأن الوظيفة تملك صلاحية كتابة الأخبار وسجل المصادر وتحديث صحة المصادر وإنشاء سجلات التشغيل. وإذا كانت متاحة للعامة، يمكن لمهاجم استهلاك موارد الجلب أو تغيير حالة البيانات أو التسبب في ضغط على المصادر والخدمات التابعة.

**الإجراء المطلوب:** فرض مصادقة خدمة إلى خدمة. الخيار المفضل هو endpoint داخلي لا يقبل إلا JWT خدمة مخصص أو توقيع HMAC يتغير دوريًا، مع تحقق صريح داخل الوظيفة. يجب كذلك تقييد حجم الطلب، وإضافة rate limit، وتسجيل هوية المستدعي، واختبار الاستدعاء المجهول والاستدعاء الصحيح في البيئة المنشورة.

### أولوية عالية: واجهة تحليل عامة مع CORS مفتوح

`supabase/functions/mirsad-analysis-api/index.ts` يعلن[3] `Access-Control-Allow-Origin: *` ويقبل GET وPOST من أي أصل. وجود `MIRSAD_API_KEY` يحمي الاتصال الداخلي بالـ API الخارجي، لكنه لا يثبت أن المستدعي نفسه مصرح له. وبذلك يمكن لأي طرف معرفة endpoint واستدعاؤه مباشرة من المتصفح أو من خادم خارجي، ما قد يستهلك الحصة أو يرسل محتوى غير موثوق إلى خدمة التحليل.

كما أن الوظيفة تقبل `source_url` وترسله إلى خدمة التحليل الخارجية التي قد تجلب الرابط عندما يكون المحتوى قصيرًا. يجب التعامل مع ذلك كسطح SSRF غير مباشر حتى لو كان التنفيذ الفعلي عند مزود آخر.

**الإجراء المطلوب:** إزالة CORS المفتوح، وقصره على الأصل الرسمي، وفرض JWT أو توقيع خدمة على POST، ووضع allowlist للمصادر أو تعطيل جلب الروابط غير الموثوقة. يجب إضافة rate limit وحصص لكل مستخدم، والتحقق من البروتوكول والمضيف، ومنع عناوين الشبكات الداخلية وإعادة التوجيه غير الموثوق.

### أولوية عالية: غياب تعريف نشر قابل لإعادة الإنتاج

لا يحتوي المستودع على `package.json` أو lockfile أو إعداد GitHub Actions أو ملف Supabase config أو إعداد نشر واضح لـ GitHub Pages أو n8n. توجد ملفات الموقع والهجرات والوظائف، لكن لا يوجد في المستودع مسار آلي يثبت أن الهجرات تُطبق بالترتيب، وأن وظائف Edge تُنشر بإعدادات JWT الصحيحة، وأن الأسرار تُحقن من بيئة آمنة.

هذا يرفع احتمال اختلاف البيئة المنشورة عن المستودع. كما يجعل العودة إلى إصدار سابق أو إعادة بناء بيئة جديدة عملية يدوية.

**الإجراء المطلوب:** إضافة CI يحقق من تركيب JavaScript وJSON، ويشغل فحصًا أمنيًا، ويتحقق من الروابط المحلية، ويطبق الهجرات في بيئة اختبار. يجب توثيق إعدادات النشر في README، مع إبقاء الأسرار خارج Git وتسجيل أسماء المتغيرات المطلوبة فقط.

### أولوية متوسطة: الاعتماد على CDN دون تثبيت إصدار أو SRI

تستورد الصفحات `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2` دون رقم إصدار دقيق ودون `integrity` و`crossorigin`. هذا يعني أن نشر إصدار جديد من المكتبة قد يغير سلوك الموقع دون commit جديد، كما أن اختراق سلسلة التوريد أو خطأ المزود يؤثر مباشرة في صفحات المصادقة والبيانات.

**الإجراء المطلوب:** تثبيت إصدار محدد، أو استضافة نسخة مراجعة محليًا ضمن عملية build، وإضافة SRI عند استخدام CDN. ينبغي تكرار الفحص عند تحديث المكتبة بدل الاعتماد على `@2` المتحرك.

### أولوية متوسطة: سطح XSS يحتاج سياسة دفاعية موحدة

يستخدم المشروع `innerHTML` في عدة ملفات لبناء عناصر واجهة، ويطبق بعض التطهير اليدوي مثل `safe()` أو تحويل النص إلى عقد DOM في مواضع أخرى. هذا النمط قابل للخطأ، خصوصًا لأن الأخبار وملخصاتها ومحتوى الملفات الشخصية تأتي من بيانات خارجية أو من المستخدم.

لم يظهر في المستودع Content Security Policy موثق أو ملف Headers للاستضافة. لذلك لا توجد طبقة دفاع ثانية واضحة إذا أخطأ مسار تطهير واحد.

**الإجراء المطلوب:** اعتماد `textContent` وواجهات DOM الآمنة افتراضيًا، وحصر `innerHTML` في قوالب ثابتة لا تحتوي بيانات غير موثوقة. يجب إضافة CSP مناسبة، ومنع `unsafe-inline` تدريجيًا، واختبار عناوين وملخصات تحتوي HTML وSVG وسمات أحداث.

### أولوية متوسطة: الحذف الدوري لا يملك طبقة أرشفة واضحة

الهجرات تحتوي عمليات لحذف الأخبار القديمة أو بيانات مصدر سابق، منها تقليص الأخبار إلى حد زمني أو عددي، وحذف بيانات `daraj`. كما تحتوي مهام Cron لتنظيف الأخبار السريعة.

هذه العمليات تبدو مقصودة، لكنها غير قابلة للاسترجاع من قاعدة البيانات بعد تنفيذها. لا يظهر في المستودع مسار backup أو archive أو اختبار استعادة.

**الإجراء المطلوب:** تعريف سياسة احتفاظ مكتوبة، وتصدير أو أرشفة قبل الحذف، واختبار استعادة دوري. يجب جعل عمليات التنظيف قابلة للمراقبة مع عدد الصفوف قبل وبعد التنفيذ، والتنبيه عند حذف رقم غير معتاد.

### أولوية منخفضة: قابلية الصيانة والاختبار محدودة

الفحص التركيبي لملفات JavaScript نجح، كما أن `n8n-workflow.json` صالح كـ JSON. لكن لا توجد اختبارات وحدة أو تكامل أو اختبارات متصفح أو lint أو typecheck للمشروع ككل. كما أن عدم وجود `package.json` يمنع تثبيت نسخة أدوات ثابتة.

النتيجة الحالية تثبت أن الملفات قابلة للقراءة نحويًا فقط. ولا تثبت أن OAuth أو RLS أو Cron أو دفع الرصيد أو مسار n8n يعمل في البيئة الحية.

**الإجراء المطلوب:** إنشاء حزمة اختبارات صغيرة تغطي استعادة الجلسة، وضع الزائر، استهلاك الفتحة مرتين بالتزامن، المكافأة اليومية، قراءة الأخبار المعلقة، طلبات الوظائف غير المصرح بها، ومسار webhook الكامل في n8n.

## ما تم التحقق منه

تم التحقق من أن شجرة Git نظيفة عند بدء الفحص، وأن المستودع على `main` ومتزامن مع `origin/main` عند الالتزام `e517be8`. تم فحص 82 ملفًا متتبعًا و30 ترحيلًا و8 وظائف Edge.

تم تشغيل `node --check` على ملفات JavaScript الأساسية، ونجحت جميع الفحوصات التي شُغلت. تم تحليل ملف `n8n-workflow.json` كـ JSON، ونجح التحليل. تم تشغيل خادم ملفات ثابت محليًا، وأعادت الصفحات الموجودة استجابات HTTP 200. لم توجد مراجع محلية مكسورة بين ملفات HTML والمصادر المحلية التي فُحصت.

هذه النتائج لا تتضمن اختبارًا حيًا على Supabase أو GitHub Pages أو n8n؛ لأن ذلك يحتاج الوصول إلى بيئة التشغيل وبياناتها الفعلية. كما لم تُرسل طلبات كتابة أو حذف إلى أي خدمة خارجية.

## خطة العمل المقترحة

### خلال يوم واحد

يجب أولًا جرد وظائف Edge المنشورة ومقارنة إعداد `verify_jwt` الفعلي مع الغرض من كل وظيفة. بعد ذلك يجب اختبار كل endpoint من دون مصادقة وبمصادقة صحيحة. يجب تقييد `mirsad-analysis-api` وإضافة rate limit، ثم تدوير أي سر خدمة سبق استخدامه في بيئة غير موثوقة.

### خلال أسبوع

ينبغي إضافة إعداد CI، وتثبيت نسخة Supabase JavaScript، وإضافة اختبارات متصفح ومسار اختبار للـ RPC. كما يجب توثيق المتغيرات السرية المطلوبة لـ Supabase وn8n وAgentMail وTurnstile، مع توثيق مصدر الجدولة الفعلي لكل وظيفة.

### قبل التوسع

يجب إنشاء مراقبة للـ Cron والاستيعاب، تشمل مدة التشغيل وعدد العناصر المرئية والمكررة والأخطاء وعدد الحذف. وينبغي إجراء اختبار استعادة من نسخة احتياطية، ومراجعة CSP وRLS بمستخدم مجهول ومستخدم عادي ومستخدم `news_admin`، ثم اعتماد تقرير اختبار قبول قبل أي زيادة في حجم الأخبار أو عدد المستخدمين.

## حدود الفحص

لم يتضمن الفحص قراءة أسرار البيئة أو طلب قيمها، ولم يتضمن تغيير قاعدة البيانات أو نشر وظائف أو تعديل إعدادات حسابات خارجية. لم يمكن من المستودع وحده إثبات إعدادات GitHub Pages أو Supabase Dashboard أو n8n الفعلية، ولذلك صيغت النتائج المتعلقة بالمصادقة والنشر بصيغة «غير مثبتة» عندما لم يكن الدليل موجودًا في الشيفرة.

## المراجع

[1]: https://github.com/MiddleEast18/momentbymoment "مستودع مِرصاد على GitHub"

[2]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/functions/mirsad-ingest/index.ts "Mirsad ingest Edge Function"

[3]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/functions/mirsad-analysis-api/index.ts "Mirsad analysis API Edge Function"

[4]: https://github.com/MiddleEast18/momentbymoment/blob/main/n8n-workflow.json "Mirsad n8n workflow"

[5]: https://github.com/MiddleEast18/momentbymoment/blob/main/index.html "Mirsad main HTML page"

[6]: https://github.com/MiddleEast18/momentbymoment/blob/main/auth-v8.js "Mirsad authentication client"

[7]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260909132920_harden_mirsad_security_policies.sql "Mirsad security policy migration"

[8]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260909173500_mirsad_rls_policy_merge.sql "Mirsad public and authenticated news RLS migration"

[9]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260912001000_add_news_view_cycles_atomic.sql "Atomic news view cycle migration"

[10]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260914214000_add_daily_reward_system.sql "Daily reward system migration"

[11]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260916012500_set_rss_ingest_every_15_minutes.sql "RSS ingestion schedule migration"

[12]: https://github.com/MiddleEast18/momentbymoment/blob/main/supabase/migrations/20260916010500_restore_oldest_first_news_trim_1000.sql "News retention and trimming migration"

[13]: https://owasp.org/www-project-top-ten/ "OWASP Top 10 Web Application Security Risks"

[14]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase Row Level Security"

[15]: https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP "MDN Content Security Policy"

[16]: https://developer.mozilla.org/en-US/docs/Web/Security/Subresource_Integrity "MDN Subresource Integrity"

[17]: https://owasp.org/www-community/attacks/Server_Side_Request_Forgery_Prevention_Cheat_Sheet "OWASP SSRF Prevention Cheat Sheet"
