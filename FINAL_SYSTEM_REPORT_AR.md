# التقرير النهائي لنظام مِرصاد

**تاريخ التقرير:** 16 سبتمبر 2026

## 1. الملخص التنفيذي

أُجريت خلال هذه الدورة مجموعة متكاملة من التعديلات والتحققات على منصة مِرصاد، مع التركيز على نظام الأخبار الملتقطة، مصدر Middle East Eye، مصادقة الزوار، أزرار «عرض المزيد» و«عرض الكل»، واستقرار الجلب والنشر. النتيجة الحالية مستقرة من ناحية النشر والتحميل والحماية الخلفية.

أصبح قسم الأخبار الملتقطة قابلًا للقراءة للزوار، ويعرض بطاقات Middle East Eye بصورة مستقلة. وفي المقابل، بقيت إجراءات التوسعة التي قد تستهلك فتحات الأخبار محمية للمستخدمين المسجلين. كما تم تصحيح رسائل الواجهة حتى لا يُعامل الزائر على أنه يملك رصيدًا غير كافٍ، وحتى لا تظهر له رسالة تحقق عامة عندما تكون المشكلة الفعلية هي غياب جلسة المصادقة.

اختبار الضغط القراءة فقط حقق معدل نجاح قدره **100%** على الصفحة وملفات الواجهة والصلاحيات ضمن الحمل المحدود المستخدم. أما اختبار مسار المستخدم المسجل، فقد تم التحقق منه برمجيًا وعلى مستوى صلاحيات قاعدة البيانات، لكن لم تُنفذ عملية خصم فعلية على حساب حقيقي لتجنب تغيير رصيد مستخدم دون طلب مستقل.

## 2. نطاق التعديلات

### 2.1 استبدال مصدر الأخبار الملتقطة

تم تعطيل مصدر Daraj واعتماد Middle East Eye بدلًا منه عبر خلاصة RSS التالية:

```text
https://www.middleeasteye.net/rss
```

المصدر الجديد مستقل عن مصادر الأخبار الرئيسية، ويستخدم المفتاح:

```text
middleeasteye
```

تم ضبط واجهة الأخبار الملتقطة لتقرأ هذا المفتاح بدلًا من `daraj`. أظهر الفحص النهائي **32 بطاقة** من Middle East Eye في قاعدة البيانات والواجهة الحية.

### 2.2 تنظيف مخلفات Daraj

تم تنفيذ تنظيف محدود النطاق حذف سجلات Daraj من جداول المصدر وسجل الالتقاط والبطاقات الملتقطة وحالة الخلاصة والصحة. كما حُذفت البطاقات الرئيسية التي تحمل رابطًا أو اسم Daraj. لم تُحذف الجداول ولم تتغير بنيتها، وبقي مصدر Middle East Eye والمصادر الأخرى دون تغيير.

نتيجة التحقق بعد التنظيف كانت صفر سجل Daraj في الجداول المتأثرة.

### 2.3 حماية سجل الالتقاط

تمت معالجة مشكلة قيد المفتاح الأجنبي في `source_item_ledger` بإضافة حارس دفاعي يمنع ربط سجل الالتقاط بمعرّف مقال رئيسي غير موجود. هذا التعديل يحمي سلامة الإدخال دون تغيير منطق الجلب الأساسي.

### 2.4 نظام الجلب والتقليم

تم اعتماد دورة جلب رئيسية كل 15 دقيقة مع زيادة مهلة المصدر إلى 20 ثانية. كما تم اعتماد نشر مستقل لكل خبر عربي صالح بدل دمج الأخبار في بطاقة واحدة. وأصبح التقليم زمنيًا، بحيث تُحذف الأخبار الأقدم أولًا مع الحفاظ على أحدث 1000 بطاقة وفق السياسة المطبقة، وحماية أحدث 500 بطاقة أثناء دورة التقليم.

### 2.5 واجهة الأخبار الملتقطة

تمت إزالة فلتر Daraj القديم من `rapid-news.js` واستبداله بفلتر Middle East Eye. كما تم الحفاظ على حد التحميل البالغ 400 سجل للواجهة، مع ترتيب الأخبار حسب وقت النشر ثم وقت الالتقاط.

### 2.6 إتاحة القراءة للزوار

كان فتح قسم الأخبار الملتقطة يستدعي دالة مصادقة مخصصة، ولذلك كان الزائر يرى رسالة تفيد بتعذر الفتح. تم تغيير `openRapid()` في ملف العرض العام ليتيح القراءة العامة، مع الإبقاء على وظائف التوسعة محمية.

النتيجة المقصودة هي:

- قراءة قسم الأخبار الملتقطة وعرض البطاقات الأولى: متاح للزائر.
- «عرض المزيد» و«عرض الكل» في الأخبار الملتقطة: يتطلبان مصادقة ورصيدًا.
- الأخبار الرئيسية: بقيت وظائف التوسعة محمية وفق منطقها الأصلي.

### 2.7 إصلاح أزرار الأخبار الرئيسية

أضيف فرع خاص بالزائر في `news-pagination.js`. يتحقق الكود من وضع الزائر عبر مفتاح التخزين المحلي أو عنصر واجهة «تسجيل الدخول» الذي يحقنه نظام المصادقة. عند اكتشاف الزائر، يتوقف التنفيذ قبل استدعاء دالة الرصيد وتظهر نافذة صغيرة توضح أن تسجيل الدخول مطلوب.

هذا الفرع لا يغير مسار المستخدم المسجل. عند وجود مستخدم مصادق عليه، يستمر الكود في استدعاء `consume_news_view`، ثم يطبق نتيجة الرصيد والخصم أو الاستخدام المجاني كما كانت.

### 2.8 معالجة التخزين المؤقت

بسبب استمرار GitHub Pages في تقديم نسخة قديمة من `view-access.js`، أُنشئ ملف مستقل باسم `view-access-public.js` ثم تم تحديث مرجع الصفحة إليه. كما تم تحديث أرقام إصدارات ملفات JavaScript حتى تصل الإصلاحات إلى المتصفح دون الاعتماد على التخزين المؤقت السابق.

## 3. اختبارات الأمان والصلاحيات

### 3.1 حماية دالة استهلاك الفتحات

تم فحص تعريف الدالة `consume_news_view(text)` في قاعدة بيانات Supabase. أظهرت النتائج:

| الاختبار | النتيجة |
|---|---:|
| تنفيذ الدالة بواسطة `anon` | غير مسموح |
| تنفيذ الدالة بواسطة `authenticated` | مسموح |
| وجود حارس `auth.uid()` | موجود |
| وجود فحص لرصيد الفتحات | موجود |

هذا يعني أن تعديل واجهة الزائر لم يفتح دالة الخصم للعامة ولم يسمح بتجاوز الرصيد عبر استدعاء مباشر من زائر مجهول.

### 3.2 وظائف القراءة العامة

قراءة بطاقات `rapid_news` تتم من الواجهة العامة باستخدام مفتاح النشر العام. أما العمليات التي تغير حالة القراءة أو تستهلك فتحات الأخبار فما زالت مرتبطة بالمصادقة. كما بقيت دالة عداد الأخبار غير المقروءة محمية للمستخدمين المسجلين.

### 3.3 التحقق من سلامة الشيفرة

تم تشغيل فحص بناء JavaScript للملفات التالية:

```text
news-pagination.js
rapid-news.js
view-access-public.js
```

وكانت النتيجة:

```text
SYNTAX_OK
```

كما بقي مستودع Git نظيفًا ومتزامنًا مع الفرع البعيد بعد آخر نشر.

## 4. اختبارات End-to-End

تم فحص النسخة الحية على `https://marsad.website/`، وتأكدت النتائج التالية:

- ظهور أزرار الأخبار الرئيسية «عرض المزيد» و«عرض الكل».
- فتح قسم الأخبار الملتقطة للزائر.
- ظهور عبارة `32 خبرًا ظاهرًا` في القسم.
- ظهور بطاقات Middle East Eye في الواجهة.
- عدم وجود بطاقات Daraj في البيانات المتحققة.
- استمرار حماية وظائف التوسعة التي تعتمد على الرصيد.
- استمرار ظهور زر «تسجيل الدخول» في وضع الزائر.

كشف الاختبار الطرفي الأول أن وجود زر «تسجيل الدخول» وحده لم يكن كافيًا دائمًا لاكتشاف وضع الزائر من مفتاح التخزين المحلي. لذلك تم توسيع شرط التعرف على الزائر ليشمل وجود عنصر `mirsadGuestExit`. نُشر هذا الإصلاح ثم تحققت النسخة الحية من استخدامه.

لم تُنفذ عملية خصم فعلية على مستخدم مسجل أثناء الاختبار، لأن ذلك كان سيغير رصيد حساب حقيقي. بدلًا من ذلك، تم التحقق من مسار الاستدعاء في الشيفرة ومن صلاحيات الدالة وحارس المصادقة وفحص الرصيد في قاعدة البيانات.

## 5. اختبار الضغط

أُجري اختبار ضغط محدود وآمن باستخدام طلبات HTTP قراءة فقط. لم يستدع الاختبار دالة الخصم ولم يغير بيانات أو أرصدة. استخدم الاختبار توازيًا يصل إلى 30 طلبًا، وفق التوزيع الآتي:

| المسار | إجمالي الطلبات | الناجحة | الفاشلة | متوسط الاستجابة | أقصى استجابة |
|---|---:|---:|---:|---:|---:|
| الصفحة الرئيسية | 60 | 60 | 0 | 2202 ms | 3308 ms |
| `news-pagination.js` | 120 | 120 | 0 | 2278 ms | 5125 ms |
| `rapid-news.js` | 120 | 120 | 0 | 2215 ms | 5561 ms |
| `view-access-public.js` | 120 | 120 | 0 | 2051 ms | 4457 ms |

بلغ معدل النجاح **100%** في جميع المسارات. ويشير ذلك إلى استقرار التحميل تحت هذا الحمل المحدود. تراوحت أزمنة الاستجابة المتوسطة بين نحو 2.05 و2.28 ثانية، بينما وصلت أعلى قيمة مسجلة إلى نحو 5.56 ثانية. هذه النتائج لا تمثل اختبارًا واسع النطاق أو اختبارًا موزعًا من عدة مناطق، لكنها تكشف عدم وجود فشل HTTP ضمن الحمل المنفذ.

## 6. سجل الالتزامات الرئيسية

| الالتزام | الغرض |
|---|---|
| `1cb7dfe` | استبدال Daraj بمصدر Middle East Eye |
| `6de9242` | تنظيف بيانات Daraj فقط |
| `0bb5579` | عرض Middle East Eye في الأخبار الملتقطة |
| `25c14c3` | إتاحة فتح الأخبار الملتقطة للزوار |
| `a5a4bff` | تجاوز التخزين المؤقت عبر ملف صلاحيات جديد |
| `ac54cae` | توضيح أخطاء الوصول في الأخبار الملتقطة |
| `a948d86` | إصلاح رسائل أزرار التوسعة للزوار |
| `7a2f438` | حصر إصلاح الزائر في أخبار الصفحة الرئيسية |
| `78b6e3a` | اكتشاف وضع الزائر بصورة أكثر موثوقية |

كما تضمنت الدورة السابقة إصلاحات مرتبطة بحارس المفتاح الأجنبي، ودورة الجلب كل 15 دقيقة، والتقليم الزمني، ونشر كل خبر عربي صالح كبطاقة مستقلة.

## 7. الملفات والوظائف المتأثرة

| الملف أو المكوّن | التغيير النهائي |
|---|---|
| `rapid-news.js` | تحميل Middle East Eye ومعالجة أخطاء التوسعة |
| `news-pagination.js` | معالجة زائر الأخبار الرئيسية قبل فحص الرصيد |
| `view-access-public.js` | قراءة عامة مع إبقاء التوسعة محمية |
| `index.html` | تحديث مراجع الملفات وأرقام التخزين المؤقت |
| `mirsad-rapid-ingest` | جلب Middle East Eye مستقلًا |
| `mirsad-ingest` | جلب رئيسي كل 15 دقيقة ونشر مستقل للبطاقات |
| `source_item_ledger` | حارس للمرجع الخارجي غير الموجود |

## 8. الحدود والملاحظات الأمنية

اختبار المستخدم المسجل تحقق من المسار البرمجي وصلاحيات قاعدة البيانات، لكنه لم يستخدم جلسة حساب حقيقية ولم يخصم فتحات فعلية. لذلك لا ينبغي اعتبار التقرير اختبار قبول نهائيًا لعرض رصيد حساب محدد، بل تحققًا من سلامة المسار وعدم فتحه للزوار.

اختبار الضغط كان محدودًا ومصدره بيئة واحدة، وركز على طلبات القراءة إلى الصفحة والملفات الثابتة. لم يكن اختبارًا لتوزيع جغرافي واسع أو لاختبار آلاف المستخدمين أو لاستدعاءات RPC متزامنة بحسابات مصادقة حقيقية.

ظهرت أثناء جلسات العمل رموز وصول Supabase في محادثة سابقة. يجب تدوير أي رمز إداري أو حساس ظهر خارج القنوات الآمنة، ثم التأكد من أن الموقع يستخدم مفاتيح النشر العامة فقط في الواجهة. لا يتضمن هذا التقرير أي قيمة سرية أو رمز وصول.

## 9. الخلاصة

النظام في حالته الحالية يحقق الفصل المطلوب بين القراءة العامة والعمليات المحمية. يستطيع الزائر قراءة الأخبار الملتقطة، بينما تبقى عمليات التوسعة واستهلاك الفتحات محمية. وتبقى صلاحيات قاعدة البيانات رافضة لتنفيذ دالة الاستهلاك بواسطة `anon`، مع استمرار فحص المصادقة والرصيد للمستخدمين المسجلين.

أظهر اختبار الضغط المحدود معدل نجاح كاملًا، وأظهر اختبار الواجهة الحية أن البطاقات الملتقطة تُعرض فعليًا. كما نُشرت آخر إصلاحات التعرف على الزائر بنجاح في الالتزام `78b6e3a`.

## المراجع

[1]: https://marsad.website/ "النسخة الحية من منصة مِرصاد"
[2]: https://github.com/MiddleEast18/momentbymoment "مستودع مشروع مِرصاد على GitHub"
[3]: https://www.middleeasteye.net/rss "خلاصة Middle East Eye RSS"
[4]: https://github.com/MiddleEast18/momentbymoment/commit/78b6e3aec3aa5c2cd6c68730f7abcc2e19cfd4b5 "آخر إصلاح لاكتشاف وضع الزائر"
[5]: https://github.com/MiddleEast18/momentbymoment/commit/6de9242daa292bb2dfc610c3143ae2fa00165775 "تنظيف بيانات Daraj فقط"
[6]: https://github.com/MiddleEast18/momentbymoment/commit/a948d86ead6056f478a66a0b57a7168cef960ca5 "إصلاح رسائل أزرار التوسعة للزوار"
